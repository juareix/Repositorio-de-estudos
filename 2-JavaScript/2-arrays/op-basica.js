let color = ['verde' , 'azul']

// retorna o tamanho do array
console.log(color.length)

// O método JavaScript toString()converte uma matriz em uma string de valores de matriz (separados por vírgula).

/*O join()método também une todos os elementos do array em uma string.

Ele se comporta exatamente como toString(), mas além disso você pode especificar o separador */

//O pop()método remove o último elemento de uma matriz

//O push()método adiciona um novo elemento a um array (no final)

/*O shift()método remove o primeiro elemento da matriz e "desloca" todos os outros elementos para um índice inferior */

/*O unshift()método adiciona um novo elemento a uma matriz (no início) e "desloca" os elementos mais antigos */

//O concat()método cria um novo array mesclando (concatenando) arrays existentes

